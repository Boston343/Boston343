# Generates pretty snake animation, using @Platane's awesome snk action
name: 🐍 Generate Snake Contribution GIF
on:
  workflow_dispatch: # Manual dispatch
  schedule:
    - cron: '0 */24 * * *' # Runs at 24:00 every day.
jobs:
  snake:
    name: Snake
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
      - name: Generate Snake
        uses: Platane/snk/svg-only@v2
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            assets/github-snake.svg
            assets/github-snake-dark.svg?palette=github-dark        
      # Commit and push the changes
      - name: Publish package
      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: ":snake: Updates Snake Contribution Animation"
#           commit_user_name: Boston343
#           commit_user_email: bot@webreaper.dev
#           commit_author: Boston343 <bot@webreaper.dev>
